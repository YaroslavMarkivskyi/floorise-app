// GET  /api/purchase/lists — active + last closed list
// POST /api/purchase/lists — close active list, open new one
export async function GET() {
  return new Response("TODO", { status: 501 })
}

export async function POST() {
  return new Response("TODO", { status: 501 })
}
